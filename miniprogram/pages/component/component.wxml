<!--pages/components/components.wxml-->
<view class="components-container">
    <text-banner source="{{bannerText}}"></text-banner>
    <block wx:for="{{componentList}}" wx:key="_id">
        <basic-list-item source="{{item}}"></basic-list-item>
    </block>
</view>